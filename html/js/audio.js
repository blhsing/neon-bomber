'use strict';
function initAudio(){if(audio)return; audio=new (window.AudioContext||window.webkitAudioContext)();noiseBuffer=audio.createBuffer(1,audio.sampleRate*.5,audio.sampleRate);let d=noiseBuffer.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=Math.random()*2-1;}
function unlockAudio(){if(!audioOn)return;initAudio();audio.resume();}
function tone(type='sine',f=300,d=.1,vol=.08,slide=1,when=0){if(!audioOn||!audio)return;let t=audio.currentTime+when,o=audio.createOscillator(),g=audio.createGain(),fil=audio.createBiquadFilter();o.type=type;o.frequency.setValueAtTime(f,t);o.frequency.exponentialRampToValueAtTime(Math.max(25,f*slide),t+d);fil.type='lowpass';fil.frequency.value=5000;g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(vol,t+.008);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(fil).connect(g).connect(audio.destination);o.start(t);o.stop(t+d+.03)}
function noise(d=.2,vol=.12,cut=900,when=0){if(!audioOn||!audio)return;let t=audio.currentTime+when,s=audio.createBufferSource(),f=audio.createBiquadFilter(),g=audio.createGain();s.buffer=noiseBuffer;f.type='lowpass';f.frequency.setValueAtTime(cut,t);f.frequency.exponentialRampToValueAtTime(80,t+d);g.gain.setValueAtTime(vol,t);g.gain.exponentialRampToValueAtTime(.0001,t+d);s.connect(f).connect(g).connect(audio.destination);s.start(t);s.stop(t+d)}
const sfx={place:()=>{tone('sine',170,.08,.05,1.6)},pickup:()=>{playCC0('pickup',.15);tone('triangle',480,.09,.05,1.7);tone('sine',760,.12,.035,1.35,.06)},boom:(mega=false)=>{noise(mega?.55:.34,mega?.22:.13,mega?700:1100);tone('sine',mega?90:120,mega?.5:.3,mega?.14:.09,.3)},hit:()=>{tone('sawtooth',180,.22,.09,.35);noise(.12,.06,1800)},kick:()=>tone('square',130,.07,.035,1.7),shield:()=>{tone('sine',720,.26,.06,.45);tone('triangle',1100,.18,.035,.7)},win:()=>{playCC0('win',.2);[392,523,659,784].forEach((f,i)=>tone('triangle',f,.32,.045,1.01,i*.12))},click:()=>{playCC0('click',.13);tone('sine',420,.04,.025,1.25)}};
function music(dt){if(!audioOn||!audio||state!=='playing'||paused)return;musicTimer-=dt;if(musicTimer<=0){musicTimer=.19;const bass=[55,55,65.4,55,73.4,65.4,49,55],lead=[220,261.6,293.7,329.6,293.7,261.6,196,246.9];let st=musicStep++%16;tone('triangle',bass[Math.floor(st/2)%8],.16,.018,1);if(st%2===1)tone('sine',lead[Math.floor(st/2)%8],.11,.012,.995);}}
